# Base configuration - includes all infrastructure components
# Components organized by service
resources:
  # Infrastructure manifests
  - db-secret.yaml
  - mongodb.yaml
  - scc.yaml

commonLabels:
  app: medi-hoot-db
  db-engine: mongodb
  environment: development

