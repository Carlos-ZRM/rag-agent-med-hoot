apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pedi-hoot.fullname" . }}-test-connection"
  labels:
    {{- include "pedi-hoot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pedi-hoot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
