apiVersion: v1
kind: Service
metadata:
  name: {{ include "pedi-hoot.fullname" . }}
  labels:
    {{- include "pedi-hoot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pedi-hoot.selectorLabels" . | nindent 4 }}
